import styled from "styled-components";

const FollowContainer = styled.section`
    height: 200px;
    width: 100%;
    background: red;
`;

const FollowInfoContainer = styled.div`
    background: blue;
`;
const FollowTitle = styled.h1`
    color: white;
    margin: 0;
    `;

const FollowText = styled.p`
    color: white;
    `;

export {FollowContainer, FollowInfoContainer, FollowTitle, FollowText};